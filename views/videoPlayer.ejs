<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Video Player with Background Image</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            width: 100vw;
            height: 100vh;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            background: black;
        }

        #image-container {
            width: 100vw;
            height: 100vh;
            overflow-x: auto;
            /* Enable horizontal scrolling */
            overflow-y: hidden;
            white-space: nowrap;
            position: absolute;
            top: 0;
            left: 0;
            overflow-x: scroll !important;
            /* Force scrollbar */
            scroll-snap-type: x mandatory;
            /* Smooth scrolling */
        }

        .bg-image {
            width: 6480px;
            height: 100vh;
            object-fit: cover;
            display: inline-block;
        }

        video {
            width: 100vw;
            height: 100vh;
            object-fit: cover;
            position: absolute;
            top: 0;
            left: 0;
            z-index: 1;
            display: none;
        }

        /* Default Video (Hidden Initially) */
        #defaultVideo {
            position: absolute;
            width: 100vw;
            height: 100vh;
            object-fit: cover;
            display: none;
            /* Initially hidden */
        }

        /* Main Video Player */
        #videoPlayer {
            position: absolute;
            width: 100vw;
            height: 100vh;
            object-fit: cover;
            display: none;
            /* Initially hidden */
            z-index: 2;
            /* Above the background */
        }
    </style>
</head>

<body>

    <!-- Background Image -->
    <div id="image-container">
        <img class="bg-image" src="/images/bg3.jpg" alt="Background">
    </div>

    <!-- Default looping video (Hidden Initially) -->
    <video id="defaultVideo" loop autoplay muted>
        <source src="/videos/default.mp4" type="video/mp4">
        Your browser does not support the video tag.
    </video>

    <!-- Main Video Player -->
    <video id="videoPlayer" controls>
        <source id="videoSource" src="" type="video/mp4">
        Your browser does not support the video tag.
    </video>

    <script src="/socket.io/socket.io.js"></script>
    <script>
        const videoPlayer = document.getElementById('videoPlayer');
        const videoSource = document.getElementById('videoSource');
        const defaultVideo = document.getElementById('defaultVideo');
        const imageContainer = document.getElementById('image-container');

        const socket = io();

        // Position mapping for each button (0-5)
        // Image width is 6480px, divided into 6 sections (0-5)
        const POSITION_MAP = {
            0: 0,      // Home position
            1: 1080,   // Position 1
            2: 2160,   // Position 2
            3: 3240,   // Position 3
            4: 4320,   // Position 4
            5: 5400    // Position 5
        };

        let currentTarget = 0;
        let startPosition = 0;

        function pauseAndHideAll() {
            console.log("⏸️ Pausing all videos");
            defaultVideo.pause();
            defaultVideo.style.display = 'none';
            videoPlayer.pause();
            videoPlayer.style.display = 'none';
        }

        function playNewVideo(videoFile) {
            pauseAndHideAll();
            videoSource.src = `/videos/${videoFile}`;
            videoPlayer.style.display = 'block';
            videoPlayer.load();
            videoPlayer.currentTime = 0;
            videoPlayer.play();
            console.log(`🎬 Playing: ${videoFile}`);
        }

        // Handle movement data from OSC via server
        // /movement { position: float }
        socket.on('movement', (data) => {
            const positionIndex = data.position;
            
            // Calculate pixel position directly (1080px per unit)
            const newScrollLeft = positionIndex * 1080;
            
            imageContainer.scrollLeft = newScrollLeft;
            console.log(`🔄 Moving to position index: ${positionIndex.toFixed(3)} (${newScrollLeft.toFixed(0)}px)`);
        });

        // Handle reached destination - play video
        socket.on('reached', (data) => {
            console.log(`📍 Reached position ${data.button}`);
            
            // Ensure we are physically at the target position
            const targetPos = POSITION_MAP[data.button];
            if (targetPos !== undefined) {
                imageContainer.scrollLeft = targetPos;
                currentTarget = data.button;
                startPosition = targetPos; // Reset start position for next move
            }
            
            socket.emit('scrollComplete');
        });

        // Handle play video command from server
        socket.on('playVideo', (data) => {
            console.log(`🎬 Play video command: ${data.videoFile}`);
            playNewVideo(data.videoFile);
        });

        // Handle stop video command
        socket.on('stopVideo', () => {
            console.log('⏹️ Stop video command received');
            pauseAndHideAll();
        });

        // Handle hide everything
        socket.on('hideEverythinginVideoScreen', () => {
            console.log('🔴 Hiding all videos, showing background');
            pauseAndHideAll();
            document.getElementById('image-container').style.display = 'block';
        });

        // When video ends
        videoPlayer.addEventListener('ended', () => {
            console.log('🎬 Video ended');
            videoPlayer.pause();
            videoPlayer.style.display = 'none';
            videoPlayer.currentTime = 0;
            socket.emit('videoEnded');
        });
    </script>
</body>

</html>